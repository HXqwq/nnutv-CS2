# 赛后评价（选手评分 + 评论）· 部署说明

比赛详情页底部的「赛后评价」板块：给本场选手打 1～5 星、匿名发评论。
数据存在 **EdgeOne Pages KV**，不需要服务器、不需要数据库。

代码位置：

| 文件 | 作用 |
|---|---|
| `functions/api/ratings.js` | 评分接口（`/api/ratings`），边缘函数 |
| `functions/api/comments.js` | 评论接口（`/api/comments`），边缘函数 |
| `components/PostMatch.js` | 前端组件（打星 + 评论） |
| `scripts/mock-api.mjs` | 本地联调用，把边缘函数跑在本地 Node 上 |

---

## 一、控制台需要做的三步

> 没做这三步时页面不会报错，只会显示「评分与评论服务尚未启用」。

### 1. 开通 KV 存储

EdgeOne 控制台 → **存储 → KV** → 「立即申请」。免费版一键开通，账户容量 1GB。

### 2. 创建命名空间

KV 页面 → 「创建命名空间」，名字随意（例如 `nnutv`）。

### 3. 绑定到本项目

进入项目详情 → **KV 存储** → 「绑定命名空间」→ 选择刚建的空间，
**变量名填 `nnutv_kv`**（这个变量名就是代码里用的名字，改了要同步改
`functions/api/*.js` 顶部的 `KV_VAR`）。

绑定后重新部署一次即可生效。

### 4.（可选）配置管理员密钥

想能删评论就在 项目设置 → **环境变量** 里加一个 `ADMIN_KEY`（自己定一个口令）。
没配的话删除接口会返回 `admin_disabled`，其它功能不受影响。

---

## 二、部署配置确认

`functions/` 目录放在**项目根目录**，平台会自动识别并部署成边缘函数
（`functions/api/ratings.js` → `/api/ratings`）。同时确认项目的：

- 构建命令：`npm run build`
- 输出目录：`out`

未命中函数路由的请求会继续走静态资源，所以现有页面完全不受影响。

---

## 三、本地调试

```bash
npm run build
node scripts/mock-api.mjs 8899      # 静态产物 + 真实边缘函数（KV 用内存模拟）
# 打开 http://127.0.0.1:8899/matches/m17
```

`mock-api.mjs` 直接 import `functions/api/*.js` 并在 Node 里执行 —— 因为边缘函数只用
Web 标准 API（`Request`/`Response`），Node 18+ 原生支持。**本地跑的就是线上那份代码**，
不是另写一份仿的。它把 KV 命名空间挂到 `globalThis.nnutv_kv` 上来模拟平台的绑定。

---

## 四、数据与限制

### 存储结构（KV key 只能含字母/数字/下划线）

| key | value |
|---|---|
| `rating_{matchId}` | `{ "p43": { n: 评分人数, s: 总分 } }` |
| `rvote_{matchId}_{投票者}` | `{ "p43": 4 }` 该人已投的分数 |
| `cmt_{matchId}` | `[{ id, nick, text, ts }]`，新的在前，最多 300 条 |
| `rlc_{限流键}` | `{ w: 窗口起点, c: 次数 }` |

### 行为约定

- **一人一票**：同一浏览器（localStorage 里的随机 id）对同一选手只算一次；
  再次点击是**改票**，平均分随之修正，不会重复计数。
- **点评限流**：每 IP 每 60 秒最多 5 条评论。
- **长度上限**：昵称 16 字、评论 300 字、单场保留 300 条。
- **最终一致性**：KV 是最终一致（跨节点最多 60 秒陈旧）。写完后自己刷新能看到最新值；
  别人的设备最多 1 分钟后一致。对评价场景足够。

### 删除一条评论

```bash
# 先查列表拿到 id
curl "https://你的域名/api/comments?match=m17"

curl -X POST "https://你的域名/api/comments" \
  -H "Content-Type: application/json" \
  -d '{"action":"del","match":"m17","id":"<评论id>","key":"<ADMIN_KEY>"}'
```

---

## 五、想改的地方

| 需求 | 改哪里 |
|---|---|
| 星级改成 10 分制 | `functions/api/ratings.js` 的 `MAX_STARS` + `components/PostMatch.js` 的 `STAR_LIST` |
| 评论上限字数 | `comments.js` 的 `MAX_TEXT` / `MAX_NICK` |
| 限流放宽/收紧 | `comments.js` 的 `RATE_WINDOW` / `RATE_MAX` |
| 评分也想覆盖选手页 | 复用 `components/PostMatch.js`，把 `matchId` 换成选手 id 即可（接口按 id 隔离，不用改） |
